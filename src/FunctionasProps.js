function FunctionasProps(props) {
    return ( 
        <div>FunctionasProps Component
            <button onClick={props.data}>Calling Data Funtion</button>
        </div>
    )
}

export default FunctionasProps